services:
  - type: web
    name: sonar-agent-proxy
    runtime: docker
    plan: free
    healthCheckPath: /health
    envVars:
      - key: PPLX_API_KEY
        sync: false # set in the Render dashboard (optional; callers can send their own bearer token)
